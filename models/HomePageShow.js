class HomePageShow {

  constructor(title, image, url, currentEpURL, currentEp) {
    this.title = title;
    this.image = image;
    this.url = url;
    this.currentEpURL = currentEpURL;
    this.currentEp = currentEp;
  }

}

module.exports = {HomePageShow}
